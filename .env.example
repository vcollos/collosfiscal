# Exemplo de .env para rodar a aplicação localmente
# ------------------------------------------------
# Copie este arquivo para ".env" na raiz do projeto e preencha os valores antes de executar.
# NÃO comite o arquivo .env com credenciais reais no repositório.
#
# Como usar:
# 1) cp .env.example .env
# 2) editar .env e preencher os valores reais
# 3) python3 test_connection.py   # para verificar conexão com o banco
#
# Observações:
# - O código da aplicação prioriza st.secrets (quando rodando no Streamlit Cloud).
#   Se estiver rodando localmente, as variáveis de ambiente (ou o .env) serão usadas.
# - Você pode fornecer DATABASE_URL completo (ex.: postgresql://user:pass@host:port/dbname)
#   ou preencher as partes abaixo (HOST/PORT/USER/PASSWORD/DB_NAME).
# - Para conexões com Supabase que exigem SSL, mantenha PG_SSLMODE=require

# --- Opção A: URL completa (evita definir as partes separadas)
# DATABASE_URL=postgresql://usuario:senha@host:porta/nome_do_banco

# --- Opção B: definir partes separadas (comum para Supabase)
SUPABASE_HOST=<SEU_SUPABASE_HOST>
SUPABASE_PORT=6543
SUPABASE_DB_NAME=postgres
SUPABASE_USER=<SEU_USUARIO_POSTGRES>
SUPABASE_PASSWORD=<SUA_SENHA_POSTGRES>

# Forçar SSL (útil para Supabase)
PG_SSLMODE=require

# --- Variáveis opcionais para o cliente Supabase (se sua app usar o SDK)
# Ex.: https://<projeto>.supabase.co
SUPABASE_URL=<SEU_SUPABASE_PROJECT_URL>
# Chave pública (anon/public) — NÃO usar service_role em apps públicos
SUPABASE_KEY=<SUA_SUPABASE_ANON_KEY>
SUPABASE_ANON_KEY=<SUA_SUPABASE_ANON_KEY>

# --- Exemplo (NÃO COMITAR):
# SUPABASE_HOST=aws-0-us-east-1.pooler.supabase.com
# SUPABASE_PORT=6543
# SUPABASE_DB_NAME=postgres
# SUPABASE_USER=postgres.pktlyzyyivuoznceiqyp
# SUPABASE_PASSWORD=So_Eu_Sei_22
# PG_SSLMODE=require
